# creating an empty queue
queue=['a','b','c']
# removing items in the queue, the follows the principle of first in first out
# thus using pop(0) 
queue.pop(0)
queue.pop(0)
queue.pop(0)

# printing items din the queue
for i in queue:
    print(i)
